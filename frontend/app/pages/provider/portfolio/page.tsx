import { ComingSoon } from "@/app/components/shared/coming-soon";

export default function ProviderPortfolioPage() {
  return (
    <ComingSoon
      title="Portfolio"
      description="Project gallery with before/after photos arrives with file storage support."
      backHref="/pages/provider"
    />
  );
}
