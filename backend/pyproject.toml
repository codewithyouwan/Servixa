[project]
name = "bestbuild-api"
version = "0.1.0"
description = "BestBuild — AI-powered construction marketplace API (MVP)"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.8",
]

[tool.uv]
# Managed with uv: `uv sync` then `uv run uvicorn app.main:app --reload`
